Currently have

build_network for elecrtrically faithful builds with all the pins.

build_dense_network for monolithic systems with ahrdcoded synapse type (delete)

build_heterogeneous which is between the two in speed: creates a separate synapse subsystem for each connection so flexible synapses but more compilation. (doesn't work.)
